// Route path constants (used by Router and NavLink)
// Kept the same export names so existing imports continue to work.
const ALL_MEETUP_PAGE = "/";
const FAVORITES_PAGE = "/favorites";
const NEW_MEETUP_PAGE = "/new";

export { ALL_MEETUP_PAGE, FAVORITES_PAGE, NEW_MEETUP_PAGE };
